; Fail fast (15s) instead of the 24h default when the network middlebox blackholes
; a packet, so a stray hang surfaces as an error. See the DB_RECONNECT_EVERY workaround
; in .env / config/database.php. Lets `artisan import:all-data` run without -d overrides.
mysqlnd.net_read_timeout=15
